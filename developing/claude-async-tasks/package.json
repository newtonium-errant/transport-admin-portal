{
  "name": "transport-admin-portal",
  "version": "1.0.0",
  "description": "RRTS Transport Admin Portal",
  "scripts": {
    "build": "echo 'No build needed for static site'",
    "start": "npx serve . -p $PORT"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
